---
import unitData from "@data/unitData.ts";
import Layout from "@layouts/Layout.astro";
import GeneralConversionCard from "@components/GeneralConvertionCard.astro";

const unitTypeList = Object.keys(unitData);
---

<Layout>
  <GeneralConversionCard title="Unit Converter" unitTypeList={unitTypeList} />
</Layout>
